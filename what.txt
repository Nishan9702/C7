echo "# C7" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Nishan9702/C7.git
git push -u origin main

## After the 1st push we need only

git add .
git commit -m "give commit"
NOTE: 1st push we don't need "git push -u origin main". we just need "git push"